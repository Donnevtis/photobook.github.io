/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./assets/js/albumsDriver.js":
/*!***********************************!*\
  !*** ./assets/js/albumsDriver.js ***!
  \***********************************/
/*! exports provided: default, albumsEvensHang */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"default\", function() { return AlbumEnv; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"albumsEvensHang\", function() { return albumsEvensHang; });\n/* harmony import */ var _script__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./script */ \"./assets/js/script.js\");\n/* harmony import */ var regenerator_runtime__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! regenerator-runtime */ \"./node_modules/regenerator-runtime/runtime.js\");\n/* harmony import */ var regenerator_runtime__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(regenerator_runtime__WEBPACK_IMPORTED_MODULE_1__);\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nfunction _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }\n\nfunction _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }\n\n\n\n\nvar AlbumSet =\n/*#__PURE__*/\nfunction () {\n  function AlbumSet() {\n    _classCallCheck(this, AlbumSet);\n  }\n\n  _createClass(AlbumSet, [{\n    key: \"open\",\n    value: function open(e) {\n      e.stopPropagation();\n      var del = e.target.nextSibling;\n      var edit = del.nextSibling;\n      del.classList.toggle('album__del_show');\n      edit.classList.toggle('album__edit_show');\n    }\n  }, {\n    key: \"close\",\n    value: function close(e) {\n      var del = e.target.querySelector('.album__del');\n      var edit = e.target.querySelector('.album__edit');\n      del.classList.remove('album__del_show');\n      edit.classList.remove('album__edit_show');\n    }\n  }, {\n    key: \"albumRemove\",\n    value: function albumRemove(title) {\n      if (!confirm(\"Are you sure you want to delete album '\".concat(title.firstChild.textContent, \"'?\\n        This operation cannot be undone.\"))) return;\n      var id = albumEnv.getAlbum(title).id;\n      var album = albumEnv.getAlbum(title).album;\n      var label = albumEnv.getAlbum(title).label;\n      this.mail = {\n        id: id\n      };\n      this.method = 'DELETE';\n      this.send().then(function (res) {\n        var counters = new _script__WEBPACK_IMPORTED_MODULE_0__[\"Counters\"](-res);\n        console.log(res);\n        counters.setGenCount();\n        album.remove();\n        label.remove();\n      }, function (err) {\n        return console.log(err);\n      });\n    }\n  }, {\n    key: \"albumRename\",\n    value: function albumRename(title, key) {\n      var _this = this;\n\n      title.classList.toggle('album__title_edit');\n\n      if (this.isEdited) {\n        this.setName(title, key);\n        return;\n      }\n\n      title.removeEventListener('mouseleave', this.close);\n      key.src = '/svg/checked.svg';\n      var buffer = document.querySelector('.input-buffer');\n      this.nameNode = title.firstChild;\n      this.name = this.nameNode.textContent;\n      this.input = document.createElement('input');\n      this.input.className = 'album__name';\n      this.input.style.background = '#C0C0C0';\n      buffer.classList.add('album__name');\n      this.input.value = this.name;\n      var width = this.nameNode.offsetWidth;\n      this.input.style.width = width + 'px';\n\n      this.input.oninput = function () {\n        buffer.innerHTML = _this.input.value;\n        _this.input.style.width = buffer.offsetWidth + 'px';\n      };\n\n      this.input.onkeyup = function (e) {\n        if (e.key !== 'Enter') return;\n\n        _this.setName(title, key);\n      };\n\n      title.prepend(this.input);\n      this.input.focus();\n      this.nameNode.hidden = true;\n      this.isEdited = true;\n    }\n  }, {\n    key: \"setName\",\n    value: function setName(title, key) {\n      var _this2 = this;\n\n      key.src = '/svg/pencil-edit.svg';\n      this.isEdited = false;\n      this.nameNode.innerHTML = this.input.value;\n      this.mail = {\n        album: title.parentNode.id,\n        newName: this.input.value\n      };\n      this.method = 'POST';\n      this.input.remove();\n      this.input = null;\n      this.nameNode.hidden = false;\n      title.addEventListener('mouseleave', this.close);\n      this.send().then(null, function () {\n        _this2.nameNode.innerHTML = _this2.mail.newName;\n      });\n    }\n  }, {\n    key: \"send\",\n    value: function send() {\n      var _this3 = this;\n\n      return regeneratorRuntime.async(function send$(_context) {\n        while (1) {\n          switch (_context.prev = _context.next) {\n            case 0:\n              _context.prev = 0;\n              _context.next = 3;\n              return regeneratorRuntime.awrap(fetch('/album', {\n                method: this.method,\n                headers: {\n                  'Accept': 'application/json',\n                  'Content-Type': 'application/json'\n                },\n                body: JSON.stringify(this.mail)\n              }).then(function (res) {\n                if (res.status != 200) {\n                  console.warn('server trouble');\n                  res.text().then(function (res) {\n                    return console.log(res);\n                  });\n                  throw res.status;\n                }\n\n                return res.text();\n              }).then(function (res) {\n                _this3.res = res;\n              }));\n\n            case 3:\n              _context.next = 8;\n              break;\n\n            case 5:\n              _context.prev = 5;\n              _context.t0 = _context[\"catch\"](0);\n              throw new Error(_context.t0);\n\n            case 8:\n            case \"end\":\n              return _context.stop();\n          }\n        }\n      }, null, this, [[0, 5]]);\n    }\n  }]);\n\n  return AlbumSet;\n}();\n\nfunction albumDisplay(e) {\n  e.stopPropagation();\n\n  if (e.target.classList.contains('album__edit')) {\n    albumSet.albumRename(this, e.target);\n    return;\n  }\n\n  if (e.target.classList.contains('album__del')) {\n    albumSet.albumRemove(this);\n    return;\n  }\n\n  if (this.classList.contains('album__title_active')) {\n    albumEnv.hide(this);\n    return;\n  }\n\n  albumEnv.show(this);\n}\n\nvar AlbumEnv =\n/*#__PURE__*/\nfunction () {\n  function AlbumEnv(el) {\n    _classCallCheck(this, AlbumEnv);\n  }\n\n  _createClass(AlbumEnv, [{\n    key: \"getAlbum\",\n    value: function getAlbum(e) {\n      this.elem = e;\n      this.album = this.elem.parentNode;\n      var id = this.album.id;\n      this.menuLabel = document.getElementsByName(id)[0];\n      this.menuInput = this.menuLabel.firstElementChild;\n      return {\n        id: id,\n        album: this.album,\n        label: this.menuLabel\n      };\n    }\n  }, {\n    key: \"show\",\n    value: function show(e) {\n      if (this.elem) {\n        this.elem.classList.remove('album__title_active');\n      }\n\n      Object(_script__WEBPACK_IMPORTED_MODULE_0__[\"grid\"])();\n      albumEnv.getAlbum(e);\n      this.elem.classList.add('album__title_active');\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"content\"].classList.add('content_mobile-show');\n      this.albumGrid = this.album.lastChild;\n      this.count = this.elem.querySelector('.album__count').textContent;\n      this.count = parseInt(this.count.substring(1));\n      this.cells = this.albumGrid.children;\n\n      if (this.count > 18 && this.cells.length != this.count) {\n        albumEnv.getFiles();\n      }\n\n      this.startValue = _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].value;\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].removeEventListener('input', _script__WEBPACK_IMPORTED_MODULE_0__[\"grid\"]);\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].addEventListener('input', this.scale.bind(this));\n      var input = new Event('input');\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].dispatchEvent(input);\n      this.menuInput.checked = true; // const click = new Event('click');\n      // this.menuLabel.dispatchEvent(click);\n\n      var link = this.elem.previousSibling.name;\n      document.location.href = \"#\".concat(link, \"i\");\n      history.pushState({}, null, '/');\n    }\n  }, {\n    key: \"scale\",\n    value: function scale(e) {\n      this.val = e.target.value;\n      var size = window.screen.width < 425 ? {\n        col: 1,\n        vw: 60\n      } : Object(_script__WEBPACK_IMPORTED_MODULE_0__[\"gridScale\"])(this.val);\n      var _iteratorNormalCompletion = true;\n      var _didIteratorError = false;\n      var _iteratorError = undefined;\n\n      try {\n        for (var _iterator = this.cells[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {\n          var cell = _step.value;\n          cell.style.display = \"block\";\n        }\n      } catch (err) {\n        _didIteratorError = true;\n        _iteratorError = err;\n      } finally {\n        try {\n          if (!_iteratorNormalCompletion && _iterator.return != null) {\n            _iterator.return();\n          }\n        } finally {\n          if (_didIteratorError) {\n            throw _iteratorError;\n          }\n        }\n      }\n\n      r = Math.ceil(r);\n      var r = this.count / size.col;\n      this.albumGrid.style.gridTemplateColumns = \"repeat(\".concat(size.col, \",1fr)\");\n      this.albumGrid.style.gridTemplateRows = \"repeat(\".concat(r, \" ,\").concat(size.vw, \"vw)\");\n    }\n  }, {\n    key: \"getFiles\",\n    value: function getFiles() {\n      var url = \"/album/\".concat(this.id);\n      fetchGet(url, albumEnv.callback);\n    }\n  }, {\n    key: \"callback\",\n    value: function callback(res) {\n      var container = document.createElement('div');\n      container.innerHTML = res;\n      var _iteratorNormalCompletion2 = true;\n      var _didIteratorError2 = false;\n      var _iteratorError2 = undefined;\n\n      try {\n        for (var _iterator2 = container.children[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {\n          node = _step2.value;\n          var cell = node.cloneNode(true);\n          this.albumGrid.append(cell);\n        }\n      } catch (err) {\n        _didIteratorError2 = true;\n        _iteratorError2 = err;\n      } finally {\n        try {\n          if (!_iteratorNormalCompletion2 && _iterator2.return != null) {\n            _iterator2.return();\n          }\n        } finally {\n          if (_didIteratorError2) {\n            throw _iteratorError2;\n          }\n        }\n      }\n\n      container.remove();\n      container = null;\n    }\n  }, {\n    key: \"hide\",\n    value: function hide(e) {\n      this.elem.classList.toggle('album__title_active');\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"content\"].classList.remove('content_mobile-show');\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].removeEventListener('input', albumEnv.scale);\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].addEventListener('input', _script__WEBPACK_IMPORTED_MODULE_0__[\"grid\"]);\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].value = +Object(_script__WEBPACK_IMPORTED_MODULE_0__[\"grid\"])();\n      _script__WEBPACK_IMPORTED_MODULE_0__[\"fillLine\"].style.width = _script__WEBPACK_IMPORTED_MODULE_0__[\"slider\"].value + 'px';\n      this.menuInput.checked = false;\n    }\n  }]);\n\n  return AlbumEnv;\n}();\n\n\nvar albumEnv = new AlbumEnv();\nvar albumSet = new AlbumSet();\nalbumsEvensHang(document.documentElement);\nfunction albumsEvensHang(elem) {\n  var albumSettings = elem.querySelectorAll('.album__settings');\n  var albumNames = elem.querySelectorAll('.album__title');\n  albumNames.forEach(function (label) {\n    label.addEventListener('click', albumDisplay, false);\n    label.addEventListener('mouseleave', albumSet.close);\n  });\n  albumSettings.forEach(function (img) {\n    img.addEventListener('click', albumSet.open);\n  });\n\n  (function setChooseEvent() {\n    var menuOptions = elem.querySelectorAll('.menu__selector');\n    menuOptions.forEach(function (label) {\n      label.addEventListener('click', chooseAlbum, false);\n    });\n  })();\n\n  function chooseAlbum(e) {\n    var _this4 = this;\n\n    e.preventDefault();\n    e.stopPropagation();\n    var albumNames = elem.querySelectorAll('.album__title');\n    albumNames.find = [].find;\n    var album = albumNames.find(function (label) {\n      return label.firstChild.innerText == _this4.innerText;\n    });\n    var click = new Event('click', {\n      bubbles: false,\n      cancelable: false\n    });\n    album.dispatchEvent(click);\n    var link = this.id.match(/\\d/g).join();\n    document.location.href = \"#\".concat(link);\n    history.pushState({}, null, '/');\n  }\n}\n\n//# sourceURL=webpack:///./assets/js/albumsDriver.js?");

/***/ }),

/***/ "./assets/js/script.js":
/*!*****************************!*\
  !*** ./assets/js/script.js ***!
  \*****************************/
/*! exports provided: slider, fillLine, sidebarButton, sidebar, content, uploadButton, menuButton, newAlButton, logout, grid, gridScale, Counters */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"slider\", function() { return slider; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"fillLine\", function() { return fillLine; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"sidebarButton\", function() { return sidebarButton; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"sidebar\", function() { return sidebar; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"content\", function() { return content; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"uploadButton\", function() { return uploadButton; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"menuButton\", function() { return menuButton; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"newAlButton\", function() { return newAlButton; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"logout\", function() { return logout; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"grid\", function() { return grid; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"gridScale\", function() { return gridScale; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"Counters\", function() { return Counters; });\n/* harmony import */ var _albumsDriver__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./albumsDriver */ \"./assets/js/albumsDriver.js\");\nfunction _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError(\"Cannot call a class as a function\"); } }\n\nfunction _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if (\"value\" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }\n\nfunction _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); return Constructor; }\n\nfunction _typeof(obj) { if (typeof Symbol === \"function\" && typeof Symbol.iterator === \"symbol\") { _typeof = function _typeof(obj) { return typeof obj; }; } else { _typeof = function _typeof(obj) { return obj && typeof Symbol === \"function\" && obj.constructor === Symbol && obj !== Symbol.prototype ? \"symbol\" : typeof obj; }; } return _typeof(obj); }\n\nfunction _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _nonIterableSpread(); }\n\nfunction _nonIterableSpread() { throw new TypeError(\"Invalid attempt to spread non-iterable instance\"); }\n\nfunction _iterableToArray(iter) { if (Symbol.iterator in Object(iter) || Object.prototype.toString.call(iter) === \"[object Arguments]\") return Array.from(iter); }\n\nfunction _arrayWithoutHoles(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = new Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } }\n\n\nvar slider = document.querySelector('.scale__slider'),\n    fillLine = document.querySelector('.scale_slider-fill'),\n    sidebarButton = document.querySelector('.sidebar-button'),\n    sidebar = document.querySelector('.sidebar'),\n    content = document.querySelector('.content'),\n    uploadButton = document.querySelector('.upload-button'),\n    menuButton = document.querySelector('.menu__button'),\n    newAlButton = document.querySelector('.menu__new-album'),\n    logout = document.querySelector('.logout-button');\nvar photoGrid = document.querySelectorAll('.album__grid');\nvar areas = []; // Fetch methods\n\nfunction fetchGet(url, callback, method) {\n  fetch(url, {\n    method: method || 'GET'\n  }).then(function (res) {\n    if (res.status !== 200) {\n      console.warn(\"Looks like a problem. Status Code: \".concat(res.status));\n      return;\n    } else {\n      return res.text();\n    }\n  }).then(callback);\n}\n\nfunction uploadFile(files, url, callback, id) {\n  var formData = new FormData();\n  var arr = [];\n  formData.append('id', id);\n\n  _toConsumableArray(files).forEach(function (file) {\n    return arr.push(file);\n  });\n\n  arr.forEach(function (file) {\n    return formData.append('file', file);\n  });\n  fetch(url, {\n    method: 'POST',\n    body: formData\n  }).then(function (res) {\n    if (res.status !== 200) {\n      console.log('Looks like there was a problem. Status Code: ' + res.status);\n    } else return res.text();\n\n    if (res.status === 300) {\n      alert('too many files');\n      console.log(res);\n    }\n\n    ;\n  }).then(callback);\n}\n\n;\nslider.addEventListener('input', lineSetPos);\n\nfunction lineSetPos(e) {\n  fillLine.style.width = e.target.value;\n} //firefox slider off\n\n\nvar FIREFOX = /Firefox/i.test(navigator.userAgent);\n\nif (FIREFOX) {\n  fillLine.style.display = \"none\";\n} //scale grid\n\n\nvar grid = setGrid();\nslider.addEventListener('input', grid);\nwindow.onload = grid();\nfunction gridScale(val) {\n  var size = {};\n\n  switch (val) {\n    case '0':\n      {\n        size.col = 6;\n        size.vw = 7.5;\n      }\n      break;\n\n    case '25':\n      {\n        size.col = 5;\n        size.vw = 10;\n      }\n      break;\n\n    case '50':\n      {\n        size.col = 4;\n        size.vw = 12.5;\n      }\n      break;\n\n    case '75':\n      {\n        size.col = 3;\n        size.vw = 16;\n      }\n      break;\n\n    case '100':\n      {\n        size.col = 2;\n        size.vw = 22;\n      }\n      break;\n  }\n\n  return size;\n}\n\nfunction setGrid(e) {\n  var val = '50';\n  if (window.screen.width < 425) return function () {};\n  return function (e) {\n    // removeArea();\n    val = _typeof(e) == 'object' ? e.target.value : val;\n    var size = gridScale(val);\n    photoGrid.forEach(function (elem) {\n      var r = 2;\n      var cells = elem.children;\n      var _iteratorNormalCompletion = true;\n      var _didIteratorError = false;\n      var _iteratorError = undefined;\n\n      try {\n        for (var _iterator = cells[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {\n          var cell = _step.value;\n          cell.style.display = 'block';\n        }\n      } catch (err) {\n        _didIteratorError = true;\n        _iteratorError = err;\n      } finally {\n        try {\n          if (!_iteratorNormalCompletion && _iterator.return != null) {\n            _iterator.return();\n          }\n        } finally {\n          if (_didIteratorError) {\n            throw _iteratorError;\n          }\n        }\n      }\n\n      if (size.col <= 4) {\n        if (cells.length > size.col * 2) {\n          for (var i = size.col * 2; i < cells.length; i++) {\n            cells[i].style.display = 'none';\n          }\n        } else {\n          r = cells.length / size.col || 1;\n          r = Math.ceil(r);\n        }\n      } else if (size.col == 5 || size.col == 6) {\n        r = 3;\n\n        if (cells.length > size.col * 3) {\n          for (var _i = size.col * 3; _i < cells.length; _i++) {\n            cells[_i].style.display = 'none';\n          }\n        } else {\n          r = cells.length / size.col;\n          r = Math.ceil(r);\n        }\n      }\n\n      elem.style.gridTemplateColumns = \"repeat(\".concat(size.col, \",1fr)\");\n      elem.style.gridTemplateRows = \"repeat(\".concat(r, \" ,\").concat(size.vw, \"vw)\");\n    });\n    return val;\n  };\n} //upload files show area\n\n\nuploadButton.addEventListener('click', showArea, {\n  once: true\n});\n\nfunction showArea(e) {\n  e.preventDefault();\n\n  if (\"ontouchstart\" in document.documentElement) {\n    uploadButton.nextSibling.click();\n    return;\n  }\n\n  photoGrid.forEach(function (elem) {\n    addUploadAreas(elem);\n  });\n  uploadButton.addEventListener('click', removeArea, {\n    once: true\n  });\n}\n\nfunction addUploadAreas(elem) {\n  var upArea = document.createElement('div');\n  upArea.classList.add('upload-area');\n  upArea.innerHTML = 'DRAG FILES HERE';\n  elem.prepend(upArea);\n  areas.push(upArea);\n  var areaInput = document.createElement('input');\n  areaInput.setAttribute('type', 'file');\n  areaInput.className = 'upload-area__input';\n  areaInput.setAttribute('accept', '.jpg, .jpeg, .png');\n  areaInput.multiple = true;\n  upArea.append(areaInput);\n  var url = 'upload/';\n  var id = elem.parentNode.id; // Appear new files function\n\n  var callback = function callback(res) {\n    removeArea();\n    var container = document.createElement('div');\n    container.innerHTML += res;\n    var cells = container.children;\n    var length = cells.length;\n\n    for (var i = 0; i < length; i++) {\n      elem.prepend(cells[0]);\n    }\n\n    var counters = new Counters(length);\n    counters.setGenCount();\n    counters.setCount(elem);\n    var input = new Event('input');\n    slider.dispatchEvent(input);\n  };\n\n  setDragDrop(upArea, url, callback, id);\n}\n\nvar Counters =\n/*#__PURE__*/\nfunction () {\n  function Counters(count) {\n    _classCallCheck(this, Counters);\n\n    this.count = count;\n  }\n\n  _createClass(Counters, [{\n    key: \"setGenCount\",\n    value: function setGenCount() {\n      this.node = document.querySelector('.profile__count');\n      this.node.innerHTML = \"\".concat(this.count + +this.prevCount(), \" files\");\n    }\n  }, {\n    key: \"setCount\",\n    value: function setCount(elem) {\n      this.node = elem.parentNode.querySelector('.album__count');\n      this.node.innerHTML = \"(\".concat(this.count + +this.prevCount(), \" photos)\");\n    }\n  }, {\n    key: \"prevCount\",\n    value: function prevCount() {\n      return this.node.innerHTML.replace(/\\D/g, '');\n    }\n  }]);\n\n  return Counters;\n}();\n\nfunction setDragDrop(elem, url, callback, title) {\n  callback = callback || null;\n  title = title || null;\n  ['dragenter', 'dragover', 'dragleave', 'drop'].forEach(function (eventName) {\n    elem.addEventListener(eventName, preventDefaults, false);\n  });\n\n  function preventDefaults(e) {\n    e.preventDefault();\n    e.stopPropagation();\n  }\n\n  ;\n  ['dragenter', 'dragover'].forEach(function (eventName) {\n    elem.addEventListener(eventName, highlight, false);\n  });\n  ['dragleave', 'drop'].forEach(function (eventName) {\n    elem.addEventListener(eventName, unhighlight, false);\n  });\n\n  function highlight(e) {\n    elem.classList.add('upload-area_highlight');\n  }\n\n  ;\n\n  function unhighlight(e) {\n    elem.classList.remove('upload-area_highlight');\n  }\n\n  ;\n  elem.addEventListener('drop', handleDrop, false);\n\n  function handleDrop(e) {\n    elem.innerHTML = '<div class=\"lds-grid\"><div></div><div></div><div></div><div></div><div></div><div></div><div></div><div></div><div></div></div>';\n    var dt = e.dataTransfer;\n    var files = dt.files;\n    uploadFile(files, url, callback, title);\n  }\n\n  ;\n}\n\nfunction removeArea(e) {\n  e.preventDefault();\n  areas.forEach(function (elem) {\n    elem.remove();\n  });\n  areas = [];\n  uploadButton.addEventListener('click', showArea, {\n    once: true\n  });\n} //NEW ALBUM CREATE\n\n\nmenuButton.addEventListener('click', showInput);\n\nfunction showInput() {\n  menuButton.removeEventListener('click', showInput);\n  menuButton.addEventListener('click', hideInput);\n  newAlButton.addEventListener('keydown', sendAlbum);\n  newAlButton.style.display = 'block';\n  newAlButton.focus();\n}\n\nfunction hideInput() {\n  newAlButton.style.display = 'none';\n  menuButton.removeEventListener('click', hideInput);\n  menuButton.addEventListener('click', showInput);\n}\n\n;\n\nfunction sendAlbum(e) {\n  var title = e.target.value.trim();\n  if (e.key !== 'Enter') return;\n  hideInput();\n  var url = '/create';\n  var init = {\n    method: 'POST',\n    headers: {\n      \"Content-Type\": \"application/json\"\n    },\n    body: JSON.stringify({\n      title: title\n    })\n  };\n  fetch(url, init).then(function (res) {\n    if (res.status !== 200) {\n      console.warn(\"Looks like a problem. Status Code: \".concat(res.status));\n      return;\n    } else {\n      return res.text();\n    }\n  }).then(function (res) {\n    showAlbum(title, res);\n  });\n  e.target.value = '';\n  e.target.removeEventListener('keydown', sendAlbum);\n}\n\nfunction showAlbum(title, res) {\n  var album = document.querySelector('.album');\n  var albumNumber = album ? +album.firstChild.name + 1 : 0;\n  var actions = document.querySelector('.actions');\n  var container = document.createElement('div');\n  container.innerHTML = res;\n  var id = container.firstChild.id;\n  container.querySelector('.album__anchor').name = albumNumber;\n  var albumGrid = container.querySelector('.album__grid');\n  Object(_albumsDriver__WEBPACK_IMPORTED_MODULE_0__[\"albumsEvensHang\"])(container.firstChild);\n  actions.after(container.firstChild);\n  addUploadAreas(albumGrid);\n  photoGrid = document.querySelectorAll('.album__grid'); // Menu item\n\n  container.innerHTML = \"<label class=\\\"menu__option\\\"id=\\\"\".concat(albumNumber, \"i\\\" name=\\\"\").concat(id, \"\\\"onclick=\\\"document.location.href='#\").concat(albumNumber, \"'\\\" >\").concat(title, \"\\n        <input class=\\\"menu__input\\\" type=\\\"radio\\\" name=\\\"albums\\\">\\n        <span class=\\\"checkmark\\\"></span></label>\");\n  newAlButton.after(container.firstChild);\n} // LOGOUT BUTTON\n\n\nlogout.addEventListener('click', leave);\n\nfunction leave() {\n  fetch('user/logout', {\n    method: 'GET'\n  }).then(function (res) {\n    if (res.status !== 200) {\n      console.warn(\"Looks like a problem. Status Code: \".concat(res.status));\n      return;\n    } else {\n      window.location.href = res.url;\n    }\n  });\n} // Redactor \n\n\nvar getRedactor = document.querySelector('.profile__avatar-button');\ngetRedactor.addEventListener('click', Redactor);\n\nfunction Redactor() {\n  fetchGet('redactor', avatarRedactor);\n\n  function avatarRedactor(box) {\n    var boxShadow = document.createElement('div');\n    boxShadow.className = 'box-wrapper';\n    boxShadow.innerHTML = box;\n    document.body.prepend(boxShadow);\n    boxShadow.addEventListener('mousedown', deleteRedactor);\n    boxShadow.addEventListener('wheel', function (e) {\n      return e.preventDefault();\n    }, false);\n    var avatarArea = document.querySelector('.photo-redactor__img');\n    var avatarFade = document.querySelector('.photo-redactor__img_fade');\n    var avatarThumb = document.querySelector('.photo-redactor__cropper');\n    var avatarCircle = document.querySelector('.photo-redactor__cropper_circle');\n    var redactor = document.querySelector('.photo-redactor');\n    var ltThumb = document.querySelector('.photo-redactor__thumb_LT');\n    var rtThumb = document.querySelector('.photo-redactor__thumb_RT');\n    var lbThumb = document.querySelector('.photo-redactor__thumb_LB');\n    var rbThumb = document.querySelector('.photo-redactor__thumb_RB');\n    var originWidth = 100;\n    var originHeight = 100;\n    var shiftOffsetX = 0;\n    var shiftOffsetY = 0;\n    var cropOffsetX = 0;\n    var cropOffsetY = 0;\n    var width = redactor.offsetWidth;\n    var height = redactor.offsetHeight;\n\n    avatarArea.onload = function (e) {\n      originWidth = e.target.naturalWidth;\n      originHeight = e.target.naturalHeight;\n\n      if (originWidth > originHeight) {\n        avatarFade.style.height = '100%';\n        avatarFade.style.width = 'auto';\n        avatarArea.style.height = height + 'px';\n        avatarArea.style.width = 'auto';\n        avatarFade.addEventListener('mousedown', shiftSE);\n      } else {\n        avatarFade.style.width = '100%';\n        avatarFade.style.height = 'auto';\n        avatarArea.style.width = width + 'px';\n        avatarArea.style.height = 'auto';\n        avatarFade.addEventListener('mousedown', shiftNW);\n      }\n    }; // Picture shift\n\n\n    function shiftSE(e) {\n      var offsetX = avatarFade.offsetLeft;\n      var cursorX = e.clientX - offsetX;\n      moveAt(e);\n\n      document.onmousemove = function (e) {\n        moveAt(e);\n      };\n\n      function moveAt(e) {\n        var x = e.clientX - cursorX;\n        var shiftX = Math.max(Math.min(x, 0), redactor.clientWidth - avatarFade.clientWidth);\n        avatarFade.style.left = shiftX + 'px';\n        shiftOffsetX = shiftX;\n        thumbShifterSE(shiftOffsetX, cropOffsetX);\n\n        document.onmouseup = function () {\n          document.onmousemove = null;\n        };\n      }\n    }\n\n    ;\n\n    function shiftNW(e) {\n      var offsetY = avatarFade.offsetTop;\n      var cursorY = e.clientY - offsetY;\n      moveAt(e);\n\n      document.onmousemove = function (e) {\n        moveAt(e);\n      };\n\n      function moveAt(e) {\n        var y = e.clientY - cursorY;\n        var shiftY = Math.max(Math.min(y, 0), redactor.clientHeight - avatarFade.clientHeight);\n        avatarFade.style.top = shiftY + 'px';\n        shiftOffsetY = shiftY;\n        thumbShifterNW(shiftOffsetY, cropOffsetY);\n\n        document.onmouseup = function () {\n          document.onmousemove = null;\n        };\n      }\n    }\n\n    ; //Thumb shift\n\n    avatarThumb.onmousedown = function (e) {\n      var startX = avatarThumb.offsetLeft;\n      var startY = avatarThumb.offsetTop;\n      var thumbWidth = avatarThumb.clientWidth;\n\n      if (e.target !== avatarArea) {\n        resizer();\n        return;\n      }\n\n      ; // Crop resize\n\n      function resizer() {\n        var cursorX = e.clientX;\n        var cursorY = e.clientY;\n        var frame = redactor.offsetWidth;\n        var right = frame - startX - thumbWidth;\n        var bottom = frame - startY - thumbWidth;\n        var s = 0;\n\n        if (e.target == ltThumb) {\n          document.onmousemove = function (e) {\n            avatarFade.style.cursor = 'nw-resize';\n            var x = cursorX - e.clientX;\n            var y = cursorY - e.clientY;\n            s = Math.max(x, y);\n            var l = startX - s;\n            var t = startY - s;\n            var w = thumbWidth + s;\n            var left = Math.min(Math.max(l, Math.max(startX - startY, 0)), frame - right - 80);\n            var top = Math.min(Math.max(t, Math.max(startY - startX, 0)), frame - bottom - 80);\n            var width = Math.min(frame - right, Math.max(w, 80));\n            var height = Math.min(frame - bottom, Math.max(w, 80));\n            resize(left, top, width, height);\n          };\n        }\n\n        if (e.target == rtThumb) {\n          document.onmousemove = function (e) {\n            avatarFade.style.cursor = 'ne-resize';\n            var x = e.clientX - cursorX;\n            var y = cursorY - e.clientY;\n            s = Math.max(x, y);\n            var t = startY - s;\n            var w = thumbWidth + s;\n            var left = startX;\n            var top = Math.min(Math.max(t, Math.max(startY - right, 0)), frame - bottom - 80);\n            var width = Math.min(frame - left, Math.max(w, 80));\n            var height = Math.min(frame - bottom, Math.max(w, 80));\n            resize(left, top, width, height);\n          };\n        }\n\n        if (e.target == rbThumb) {\n          document.onmousemove = function (e) {\n            avatarFade.style.cursor = 'se-resize';\n            var x = e.clientX - cursorX;\n            var y = e.clientY - cursorY;\n            s = Math.max(x, y);\n            var left = startX;\n            var top = startY;\n            var w = thumbWidth + s;\n            var width = Math.min(frame - left, Math.max(w, 80));\n            var height = Math.min(frame - top, Math.max(w, 80));\n            resize(left, top, width, height);\n          };\n        }\n\n        if (e.target == lbThumb) {\n          document.onmousemove = function (e) {\n            avatarFade.style.cursor = 'sw-resize';\n            var x = cursorX - e.clientX;\n            var y = e.clientY - cursorY;\n            s = Math.max(x, y);\n            var l = startX - s;\n            var top = startY;\n            var w = thumbWidth + s;\n            var left = Math.min(Math.max(l, Math.max(startX - bottom, 0)), frame - right - 80);\n            var width = Math.min(frame - right, Math.max(w, 80));\n            var height = Math.min(frame - top, Math.max(w, 80));\n            resize(left, top, width, height);\n          };\n        }\n\n        function resize(left, top, width, height) {\n          avatarThumb.style.left = left + 'px';\n          avatarThumb.style.top = top + 'px';\n          avatarArea.style.left = -left - -shiftOffsetX + 'px';\n          avatarArea.style.top = -top - -shiftOffsetY + 'px';\n          avatarThumb.style.width = Math.min(width, height) + 'px';\n          avatarThumb.style.height = Math.min(width, height) + 'px';\n          avatarCircle.style.width = Math.min(width, height) + 'px';\n          avatarCircle.style.height = Math.min(width, height) + 'px';\n        }\n\n        document.onmouseup = function () {\n          avatarFade.style.cursor = 'grab';\n          if (e.target == ltThumb || e.target == lbThumb) cropOffsetX += s;\n          if (e.target == ltThumb || e.target == rtThumb) cropOffsetY += s;\n          document.onmousemove = null;\n        };\n      }\n\n      var leftBoard = redactor.getBoundingClientRect().left;\n      var rightBoard = redactor.clientWidth;\n      var topBoard = redactor.getBoundingClientRect().top;\n      var bottomBoard = redactor.clientHeight;\n      var offsetX = avatarThumb.getBoundingClientRect().left;\n      var offsetY = avatarThumb.getBoundingClientRect().top;\n      var cursorX = e.clientX;\n      var cursorY = e.clientY;\n      var cursorOffX = cursorX - offsetX;\n      var cursorOffY = cursorY - offsetY;\n\n      document.onmousemove = function (e) {\n        moveAt(e);\n      };\n\n      function moveAt(e) {\n        var cursorX = e.clientX;\n        var cursorY = e.clientY;\n        var x = cursorX - leftBoard - cursorOffX;\n        x = Math.max(Math.min(x, rightBoard - thumbWidth), 0);\n        avatarThumb.style.left = x + 'px';\n        var y = cursorY - topBoard - cursorOffY;\n        y = Math.max(Math.min(y, bottomBoard - thumbWidth), 0);\n        avatarThumb.style.top = y + 'px';\n        cropOffsetX = -x;\n        thumbShifterSE(shiftOffsetX, cropOffsetX);\n        cropOffsetY = -y;\n        thumbShifterNW(shiftOffsetY, cropOffsetY);\n\n        document.onmouseup = function () {\n          document.onmousemove = null;\n        };\n      }\n    };\n\n    function thumbShifterSE(a, b) {\n      avatarArea.style.left = a + b + 'px';\n    }\n\n    function thumbShifterNW(a, b) {\n      avatarArea.style.top = a + b + 'px';\n    }\n\n    function getCoords() {\n      var coords = {};\n      var scale = originWidth / avatarArea.clientWidth;\n      coords.width = avatarThumb.clientWidth * scale;\n      coords.left = (avatarThumb.offsetLeft - avatarFade.offsetLeft) * scale;\n      coords.top = (avatarThumb.offsetTop - avatarFade.offsetTop) * scale;\n      return coords;\n    } // Avatar uploader \n\n\n    var avatarInput = document.querySelector('.inputfile[type=file]');\n    var avatarDropArea = document.querySelector('.avatar-drop-area');\n    var path = '/redactor/upload';\n    setDragDrop(avatarDropArea, path, function (res) {\n      if (!res) return;\n      deleteRedactor();\n      avatarRedactor(res);\n    });\n\n    avatarInput.oninput = function (e) {\n      var file = e.srcElement.files;\n      uploadFile(file, path, function (res) {\n        if (!res) return;\n        deleteRedactor();\n        avatarRedactor(res);\n      });\n    }; //Avatar saver\n\n\n    var seveAvatar = document.querySelector('.inputfile[type=submit]');\n\n    seveAvatar.onclick = function () {\n      var coords = getCoords();\n      fetch('/redactor/save', {\n        method: 'POST',\n        headers: {\n          'Accept': 'application/json',\n          'Content-Type': 'application/json'\n        },\n        body: JSON.stringify(coords)\n      }).then(function (res) {\n        if (res.status == 201) {\n          var ava = document.querySelector('.profile__avatar');\n          var r = (Math.random() * 1E10).toString(32);\n          ava.src = \"/redactor/avatar/\".concat(r);\n          deleteRedactor();\n        }\n      });\n    };\n  }\n}\n\n; // Redactor ereaser\n\nfunction deleteRedactor(e) {\n  var boxShadow = document.querySelector('.box-wrapper');\n  if (e && e.target !== boxShadow) return;\n  boxShadow.remove();\n  boxShadow = null;\n}\n\ndocument.ondragstart = function () {\n  return false;\n};\n\n//# sourceURL=webpack:///./assets/js/script.js?");

/***/ }),

/***/ "./main.js":
/*!*****************!*\
  !*** ./main.js ***!
  \*****************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _assets_js_script__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./assets/js/script */ \"./assets/js/script.js\");\n/* harmony import */ var _assets_js_albumsDriver__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./assets/js/albumsDriver */ \"./assets/js/albumsDriver.js\");\n\n\n\n//# sourceURL=webpack:///./main.js?");

/***/ }),

/***/ "./node_modules/regenerator-runtime/runtime.js":
/*!*****************************************************!*\
  !*** ./node_modules/regenerator-runtime/runtime.js ***!
  \*****************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("/**\n * Copyright (c) 2014-present, Facebook, Inc.\n *\n * This source code is licensed under the MIT license found in the\n * LICENSE file in the root directory of this source tree.\n */\nvar runtime = function (exports) {\n  \"use strict\";\n\n  var Op = Object.prototype;\n  var hasOwn = Op.hasOwnProperty;\n  var undefined; // More compressible than void 0.\n\n  var $Symbol = typeof Symbol === \"function\" ? Symbol : {};\n  var iteratorSymbol = $Symbol.iterator || \"@@iterator\";\n  var asyncIteratorSymbol = $Symbol.asyncIterator || \"@@asyncIterator\";\n  var toStringTagSymbol = $Symbol.toStringTag || \"@@toStringTag\";\n\n  function wrap(innerFn, outerFn, self, tryLocsList) {\n    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.\n    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;\n    var generator = Object.create(protoGenerator.prototype);\n    var context = new Context(tryLocsList || []); // The ._invoke method unifies the implementations of the .next,\n    // .throw, and .return methods.\n\n    generator._invoke = makeInvokeMethod(innerFn, self, context);\n    return generator;\n  }\n\n  exports.wrap = wrap; // Try/catch helper to minimize deoptimizations. Returns a completion\n  // record like context.tryEntries[i].completion. This interface could\n  // have been (and was previously) designed to take a closure to be\n  // invoked without arguments, but in all the cases we care about we\n  // already have an existing method we want to call, so there's no need\n  // to create a new function object. We can even get away with assuming\n  // the method takes exactly one argument, since that happens to be true\n  // in every case, so we don't have to touch the arguments object. The\n  // only additional allocation required is the completion record, which\n  // has a stable shape and so hopefully should be cheap to allocate.\n\n  function tryCatch(fn, obj, arg) {\n    try {\n      return {\n        type: \"normal\",\n        arg: fn.call(obj, arg)\n      };\n    } catch (err) {\n      return {\n        type: \"throw\",\n        arg: err\n      };\n    }\n  }\n\n  var GenStateSuspendedStart = \"suspendedStart\";\n  var GenStateSuspendedYield = \"suspendedYield\";\n  var GenStateExecuting = \"executing\";\n  var GenStateCompleted = \"completed\"; // Returning this object from the innerFn has the same effect as\n  // breaking out of the dispatch switch statement.\n\n  var ContinueSentinel = {}; // Dummy constructor functions that we use as the .constructor and\n  // .constructor.prototype properties for functions that return Generator\n  // objects. For full spec compliance, you may wish to configure your\n  // minifier not to mangle the names of these two functions.\n\n  function Generator() {}\n\n  function GeneratorFunction() {}\n\n  function GeneratorFunctionPrototype() {} // This is a polyfill for %IteratorPrototype% for environments that\n  // don't natively support it.\n\n\n  var IteratorPrototype = {};\n\n  IteratorPrototype[iteratorSymbol] = function () {\n    return this;\n  };\n\n  var getProto = Object.getPrototypeOf;\n  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));\n\n  if (NativeIteratorPrototype && NativeIteratorPrototype !== Op && hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {\n    // This environment has a native %IteratorPrototype%; use it instead\n    // of the polyfill.\n    IteratorPrototype = NativeIteratorPrototype;\n  }\n\n  var Gp = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(IteratorPrototype);\n  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;\n  GeneratorFunctionPrototype.constructor = GeneratorFunction;\n  GeneratorFunctionPrototype[toStringTagSymbol] = GeneratorFunction.displayName = \"GeneratorFunction\"; // Helper for defining the .next, .throw, and .return methods of the\n  // Iterator interface in terms of a single ._invoke method.\n\n  function defineIteratorMethods(prototype) {\n    [\"next\", \"throw\", \"return\"].forEach(function (method) {\n      prototype[method] = function (arg) {\n        return this._invoke(method, arg);\n      };\n    });\n  }\n\n  exports.isGeneratorFunction = function (genFun) {\n    var ctor = typeof genFun === \"function\" && genFun.constructor;\n    return ctor ? ctor === GeneratorFunction || // For the native GeneratorFunction constructor, the best we can\n    // do is to check its .name property.\n    (ctor.displayName || ctor.name) === \"GeneratorFunction\" : false;\n  };\n\n  exports.mark = function (genFun) {\n    if (Object.setPrototypeOf) {\n      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);\n    } else {\n      genFun.__proto__ = GeneratorFunctionPrototype;\n\n      if (!(toStringTagSymbol in genFun)) {\n        genFun[toStringTagSymbol] = \"GeneratorFunction\";\n      }\n    }\n\n    genFun.prototype = Object.create(Gp);\n    return genFun;\n  }; // Within the body of any async function, `await x` is transformed to\n  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test\n  // `hasOwn.call(value, \"__await\")` to determine if the yielded value is\n  // meant to be awaited.\n\n\n  exports.awrap = function (arg) {\n    return {\n      __await: arg\n    };\n  };\n\n  function AsyncIterator(generator) {\n    function invoke(method, arg, resolve, reject) {\n      var record = tryCatch(generator[method], generator, arg);\n\n      if (record.type === \"throw\") {\n        reject(record.arg);\n      } else {\n        var result = record.arg;\n        var value = result.value;\n\n        if (value && typeof value === \"object\" && hasOwn.call(value, \"__await\")) {\n          return Promise.resolve(value.__await).then(function (value) {\n            invoke(\"next\", value, resolve, reject);\n          }, function (err) {\n            invoke(\"throw\", err, resolve, reject);\n          });\n        }\n\n        return Promise.resolve(value).then(function (unwrapped) {\n          // When a yielded Promise is resolved, its final value becomes\n          // the .value of the Promise<{value,done}> result for the\n          // current iteration.\n          result.value = unwrapped;\n          resolve(result);\n        }, function (error) {\n          // If a rejected Promise was yielded, throw the rejection back\n          // into the async generator function so it can be handled there.\n          return invoke(\"throw\", error, resolve, reject);\n        });\n      }\n    }\n\n    var previousPromise;\n\n    function enqueue(method, arg) {\n      function callInvokeWithMethodAndArg() {\n        return new Promise(function (resolve, reject) {\n          invoke(method, arg, resolve, reject);\n        });\n      }\n\n      return previousPromise = // If enqueue has been called before, then we want to wait until\n      // all previous Promises have been resolved before calling invoke,\n      // so that results are always delivered in the correct order. If\n      // enqueue has not been called before, then it is important to\n      // call invoke immediately, without waiting on a callback to fire,\n      // so that the async generator function has the opportunity to do\n      // any necessary setup in a predictable way. This predictability\n      // is why the Promise constructor synchronously invokes its\n      // executor callback, and why async functions synchronously\n      // execute code before the first await. Since we implement simple\n      // async functions in terms of async generators, it is especially\n      // important to get this right, even though it requires care.\n      previousPromise ? previousPromise.then(callInvokeWithMethodAndArg, // Avoid propagating failures to Promises returned by later\n      // invocations of the iterator.\n      callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg();\n    } // Define the unified helper method that is used to implement .next,\n    // .throw, and .return (see defineIteratorMethods).\n\n\n    this._invoke = enqueue;\n  }\n\n  defineIteratorMethods(AsyncIterator.prototype);\n\n  AsyncIterator.prototype[asyncIteratorSymbol] = function () {\n    return this;\n  };\n\n  exports.AsyncIterator = AsyncIterator; // Note that simple async functions are implemented on top of\n  // AsyncIterator objects; they just return a Promise for the value of\n  // the final result produced by the iterator.\n\n  exports.async = function (innerFn, outerFn, self, tryLocsList) {\n    var iter = new AsyncIterator(wrap(innerFn, outerFn, self, tryLocsList));\n    return exports.isGeneratorFunction(outerFn) ? iter // If outerFn is a generator, return the full iterator.\n    : iter.next().then(function (result) {\n      return result.done ? result.value : iter.next();\n    });\n  };\n\n  function makeInvokeMethod(innerFn, self, context) {\n    var state = GenStateSuspendedStart;\n    return function invoke(method, arg) {\n      if (state === GenStateExecuting) {\n        throw new Error(\"Generator is already running\");\n      }\n\n      if (state === GenStateCompleted) {\n        if (method === \"throw\") {\n          throw arg;\n        } // Be forgiving, per 25.3.3.3.3 of the spec:\n        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume\n\n\n        return doneResult();\n      }\n\n      context.method = method;\n      context.arg = arg;\n\n      while (true) {\n        var delegate = context.delegate;\n\n        if (delegate) {\n          var delegateResult = maybeInvokeDelegate(delegate, context);\n\n          if (delegateResult) {\n            if (delegateResult === ContinueSentinel) continue;\n            return delegateResult;\n          }\n        }\n\n        if (context.method === \"next\") {\n          // Setting context._sent for legacy support of Babel's\n          // function.sent implementation.\n          context.sent = context._sent = context.arg;\n        } else if (context.method === \"throw\") {\n          if (state === GenStateSuspendedStart) {\n            state = GenStateCompleted;\n            throw context.arg;\n          }\n\n          context.dispatchException(context.arg);\n        } else if (context.method === \"return\") {\n          context.abrupt(\"return\", context.arg);\n        }\n\n        state = GenStateExecuting;\n        var record = tryCatch(innerFn, self, context);\n\n        if (record.type === \"normal\") {\n          // If an exception is thrown from innerFn, we leave state ===\n          // GenStateExecuting and loop back for another invocation.\n          state = context.done ? GenStateCompleted : GenStateSuspendedYield;\n\n          if (record.arg === ContinueSentinel) {\n            continue;\n          }\n\n          return {\n            value: record.arg,\n            done: context.done\n          };\n        } else if (record.type === \"throw\") {\n          state = GenStateCompleted; // Dispatch the exception by looping back around to the\n          // context.dispatchException(context.arg) call above.\n\n          context.method = \"throw\";\n          context.arg = record.arg;\n        }\n      }\n    };\n  } // Call delegate.iterator[context.method](context.arg) and handle the\n  // result, either by returning a { value, done } result from the\n  // delegate iterator, or by modifying context.method and context.arg,\n  // setting context.delegate to null, and returning the ContinueSentinel.\n\n\n  function maybeInvokeDelegate(delegate, context) {\n    var method = delegate.iterator[context.method];\n\n    if (method === undefined) {\n      // A .throw or .return when the delegate iterator has no .throw\n      // method always terminates the yield* loop.\n      context.delegate = null;\n\n      if (context.method === \"throw\") {\n        // Note: [\"return\"] must be used for ES3 parsing compatibility.\n        if (delegate.iterator[\"return\"]) {\n          // If the delegate iterator has a return method, give it a\n          // chance to clean up.\n          context.method = \"return\";\n          context.arg = undefined;\n          maybeInvokeDelegate(delegate, context);\n\n          if (context.method === \"throw\") {\n            // If maybeInvokeDelegate(context) changed context.method from\n            // \"return\" to \"throw\", let that override the TypeError below.\n            return ContinueSentinel;\n          }\n        }\n\n        context.method = \"throw\";\n        context.arg = new TypeError(\"The iterator does not provide a 'throw' method\");\n      }\n\n      return ContinueSentinel;\n    }\n\n    var record = tryCatch(method, delegate.iterator, context.arg);\n\n    if (record.type === \"throw\") {\n      context.method = \"throw\";\n      context.arg = record.arg;\n      context.delegate = null;\n      return ContinueSentinel;\n    }\n\n    var info = record.arg;\n\n    if (!info) {\n      context.method = \"throw\";\n      context.arg = new TypeError(\"iterator result is not an object\");\n      context.delegate = null;\n      return ContinueSentinel;\n    }\n\n    if (info.done) {\n      // Assign the result of the finished delegate to the temporary\n      // variable specified by delegate.resultName (see delegateYield).\n      context[delegate.resultName] = info.value; // Resume execution at the desired location (see delegateYield).\n\n      context.next = delegate.nextLoc; // If context.method was \"throw\" but the delegate handled the\n      // exception, let the outer generator proceed normally. If\n      // context.method was \"next\", forget context.arg since it has been\n      // \"consumed\" by the delegate iterator. If context.method was\n      // \"return\", allow the original .return call to continue in the\n      // outer generator.\n\n      if (context.method !== \"return\") {\n        context.method = \"next\";\n        context.arg = undefined;\n      }\n    } else {\n      // Re-yield the result returned by the delegate method.\n      return info;\n    } // The delegate iterator is finished, so forget it and continue with\n    // the outer generator.\n\n\n    context.delegate = null;\n    return ContinueSentinel;\n  } // Define Generator.prototype.{next,throw,return} in terms of the\n  // unified ._invoke helper method.\n\n\n  defineIteratorMethods(Gp);\n  Gp[toStringTagSymbol] = \"Generator\"; // A Generator should always return itself as the iterator object when the\n  // @@iterator function is called on it. Some browsers' implementations of the\n  // iterator prototype chain incorrectly implement this, causing the Generator\n  // object to not be returned from this call. This ensures that doesn't happen.\n  // See https://github.com/facebook/regenerator/issues/274 for more details.\n\n  Gp[iteratorSymbol] = function () {\n    return this;\n  };\n\n  Gp.toString = function () {\n    return \"[object Generator]\";\n  };\n\n  function pushTryEntry(locs) {\n    var entry = {\n      tryLoc: locs[0]\n    };\n\n    if (1 in locs) {\n      entry.catchLoc = locs[1];\n    }\n\n    if (2 in locs) {\n      entry.finallyLoc = locs[2];\n      entry.afterLoc = locs[3];\n    }\n\n    this.tryEntries.push(entry);\n  }\n\n  function resetTryEntry(entry) {\n    var record = entry.completion || {};\n    record.type = \"normal\";\n    delete record.arg;\n    entry.completion = record;\n  }\n\n  function Context(tryLocsList) {\n    // The root entry object (effectively a try statement without a catch\n    // or a finally block) gives us a place to store values thrown from\n    // locations where there is no enclosing try statement.\n    this.tryEntries = [{\n      tryLoc: \"root\"\n    }];\n    tryLocsList.forEach(pushTryEntry, this);\n    this.reset(true);\n  }\n\n  exports.keys = function (object) {\n    var keys = [];\n\n    for (var key in object) {\n      keys.push(key);\n    }\n\n    keys.reverse(); // Rather than returning an object with a next method, we keep\n    // things simple and return the next function itself.\n\n    return function next() {\n      while (keys.length) {\n        var key = keys.pop();\n\n        if (key in object) {\n          next.value = key;\n          next.done = false;\n          return next;\n        }\n      } // To avoid creating an additional object, we just hang the .value\n      // and .done properties off the next function object itself. This\n      // also ensures that the minifier will not anonymize the function.\n\n\n      next.done = true;\n      return next;\n    };\n  };\n\n  function values(iterable) {\n    if (iterable) {\n      var iteratorMethod = iterable[iteratorSymbol];\n\n      if (iteratorMethod) {\n        return iteratorMethod.call(iterable);\n      }\n\n      if (typeof iterable.next === \"function\") {\n        return iterable;\n      }\n\n      if (!isNaN(iterable.length)) {\n        var i = -1,\n            next = function next() {\n          while (++i < iterable.length) {\n            if (hasOwn.call(iterable, i)) {\n              next.value = iterable[i];\n              next.done = false;\n              return next;\n            }\n          }\n\n          next.value = undefined;\n          next.done = true;\n          return next;\n        };\n\n        return next.next = next;\n      }\n    } // Return an iterator with no values.\n\n\n    return {\n      next: doneResult\n    };\n  }\n\n  exports.values = values;\n\n  function doneResult() {\n    return {\n      value: undefined,\n      done: true\n    };\n  }\n\n  Context.prototype = {\n    constructor: Context,\n    reset: function (skipTempReset) {\n      this.prev = 0;\n      this.next = 0; // Resetting context._sent for legacy support of Babel's\n      // function.sent implementation.\n\n      this.sent = this._sent = undefined;\n      this.done = false;\n      this.delegate = null;\n      this.method = \"next\";\n      this.arg = undefined;\n      this.tryEntries.forEach(resetTryEntry);\n\n      if (!skipTempReset) {\n        for (var name in this) {\n          // Not sure about the optimal order of these conditions:\n          if (name.charAt(0) === \"t\" && hasOwn.call(this, name) && !isNaN(+name.slice(1))) {\n            this[name] = undefined;\n          }\n        }\n      }\n    },\n    stop: function () {\n      this.done = true;\n      var rootEntry = this.tryEntries[0];\n      var rootRecord = rootEntry.completion;\n\n      if (rootRecord.type === \"throw\") {\n        throw rootRecord.arg;\n      }\n\n      return this.rval;\n    },\n    dispatchException: function (exception) {\n      if (this.done) {\n        throw exception;\n      }\n\n      var context = this;\n\n      function handle(loc, caught) {\n        record.type = \"throw\";\n        record.arg = exception;\n        context.next = loc;\n\n        if (caught) {\n          // If the dispatched exception was caught by a catch block,\n          // then let that catch block handle the exception normally.\n          context.method = \"next\";\n          context.arg = undefined;\n        }\n\n        return !!caught;\n      }\n\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n        var record = entry.completion;\n\n        if (entry.tryLoc === \"root\") {\n          // Exception thrown outside of any try block that could handle\n          // it, so set the completion value of the entire function to\n          // throw the exception.\n          return handle(\"end\");\n        }\n\n        if (entry.tryLoc <= this.prev) {\n          var hasCatch = hasOwn.call(entry, \"catchLoc\");\n          var hasFinally = hasOwn.call(entry, \"finallyLoc\");\n\n          if (hasCatch && hasFinally) {\n            if (this.prev < entry.catchLoc) {\n              return handle(entry.catchLoc, true);\n            } else if (this.prev < entry.finallyLoc) {\n              return handle(entry.finallyLoc);\n            }\n          } else if (hasCatch) {\n            if (this.prev < entry.catchLoc) {\n              return handle(entry.catchLoc, true);\n            }\n          } else if (hasFinally) {\n            if (this.prev < entry.finallyLoc) {\n              return handle(entry.finallyLoc);\n            }\n          } else {\n            throw new Error(\"try statement without catch or finally\");\n          }\n        }\n      }\n    },\n    abrupt: function (type, arg) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n\n        if (entry.tryLoc <= this.prev && hasOwn.call(entry, \"finallyLoc\") && this.prev < entry.finallyLoc) {\n          var finallyEntry = entry;\n          break;\n        }\n      }\n\n      if (finallyEntry && (type === \"break\" || type === \"continue\") && finallyEntry.tryLoc <= arg && arg <= finallyEntry.finallyLoc) {\n        // Ignore the finally entry if control is not jumping to a\n        // location outside the try/catch block.\n        finallyEntry = null;\n      }\n\n      var record = finallyEntry ? finallyEntry.completion : {};\n      record.type = type;\n      record.arg = arg;\n\n      if (finallyEntry) {\n        this.method = \"next\";\n        this.next = finallyEntry.finallyLoc;\n        return ContinueSentinel;\n      }\n\n      return this.complete(record);\n    },\n    complete: function (record, afterLoc) {\n      if (record.type === \"throw\") {\n        throw record.arg;\n      }\n\n      if (record.type === \"break\" || record.type === \"continue\") {\n        this.next = record.arg;\n      } else if (record.type === \"return\") {\n        this.rval = this.arg = record.arg;\n        this.method = \"return\";\n        this.next = \"end\";\n      } else if (record.type === \"normal\" && afterLoc) {\n        this.next = afterLoc;\n      }\n\n      return ContinueSentinel;\n    },\n    finish: function (finallyLoc) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n\n        if (entry.finallyLoc === finallyLoc) {\n          this.complete(entry.completion, entry.afterLoc);\n          resetTryEntry(entry);\n          return ContinueSentinel;\n        }\n      }\n    },\n    \"catch\": function (tryLoc) {\n      for (var i = this.tryEntries.length - 1; i >= 0; --i) {\n        var entry = this.tryEntries[i];\n\n        if (entry.tryLoc === tryLoc) {\n          var record = entry.completion;\n\n          if (record.type === \"throw\") {\n            var thrown = record.arg;\n            resetTryEntry(entry);\n          }\n\n          return thrown;\n        }\n      } // The context.catch method must only be called with a location\n      // argument that corresponds to a known catch block.\n\n\n      throw new Error(\"illegal catch attempt\");\n    },\n    delegateYield: function (iterable, resultName, nextLoc) {\n      this.delegate = {\n        iterator: values(iterable),\n        resultName: resultName,\n        nextLoc: nextLoc\n      };\n\n      if (this.method === \"next\") {\n        // Deliberately forget the last sent value so that we don't\n        // accidentally pass it on to the delegate.\n        this.arg = undefined;\n      }\n\n      return ContinueSentinel;\n    }\n  }; // Regardless of whether this script is executing as a CommonJS module\n  // or not, return the runtime object so that we can declare the variable\n  // regeneratorRuntime in the outer scope, which allows this module to be\n  // injected easily by `bin/regenerator --include-runtime script.js`.\n\n  return exports;\n}( // If this script is executing as a CommonJS module, use module.exports\n// as the regeneratorRuntime namespace. Otherwise create a new empty\n// object. Either way, the resulting object will be used to initialize\n// the regeneratorRuntime variable at the top of this file.\n true ? module.exports : undefined);\n\ntry {\n  regeneratorRuntime = runtime;\n} catch (accidentalStrictMode) {\n  // This module should not be running in strict mode, so the above\n  // assignment should always work unless something is misconfigured. Just\n  // in case runtime.js accidentally runs in strict mode, we can escape\n  // strict mode using a global Function call. This could conceivably fail\n  // if a Content Security Policy forbids using Function, but in that case\n  // the proper solution is to fix the accidental strict mode problem. If\n  // you've misconfigured your bundler to force strict mode and applied a\n  // CSP to forbid Function, and you're not willing to fix either of those\n  // problems, please detail your unique predicament in a GitHub issue.\n  Function(\"r\", \"regeneratorRuntime = r\")(runtime);\n}\n\n//# sourceURL=webpack:///./node_modules/regenerator-runtime/runtime.js?");

/***/ })

/******/ });